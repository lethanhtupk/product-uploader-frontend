import React from 'react'

const LoginPage = ({ title }: { title: string }) => {
  return (
    <>
      <header>
        <title>{title}</title>
      </header>
      <div>Login page</div>
    </>
  )
}

export default LoginPage
